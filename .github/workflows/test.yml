name: ci

on:
  push:
    branches: [master]
    paths-ignore:
      - "**/*.md"
      - ".plans/**"
  pull_request:
    paths-ignore:
      - "**/*.md"
      - ".plans/**"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: lint · format · type-check · test · build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm format:check

      - run: pnpm type-check

      - run: pnpm test

      - run: pnpm build

  e2e:
    name: connectivity (postgres + server)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_PASSWORD: mypassword
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    env:
      NODE_ENV: development
      DATABASE_URL: postgres://postgres:mypassword@localhost:5432/postgres
      # Placeholder — server requires it to boot; not actually used by the
      # connectivity tests, which don't authenticate.
      BETTER_AUTH_SECRET: ci-placeholder-secret-do-not-reuse
      BETTER_AUTH_URL: http://localhost:5173
      CORS_ORIGIN: http://localhost:5173
      MET_USER_AGENT: minhytteapp-ci/1.0 ci@example.com
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm db:migrate

      - name: Start API server in background
        run: nohup pnpm start > server.log 2>&1 &

      - name: Wait for /health
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:3001/health >/dev/null; then
              echo "server is up"
              exit 0
            fi
            sleep 1
          done
          echo "server did not start within 30s — dumping log"
          cat server.log || true
          exit 1

      - run: pnpm test:e2e

      - name: Upload server log on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: server-log
          path: server.log
          if-no-files-found: ignore
